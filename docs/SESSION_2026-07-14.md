# Session 2026-07-14 — WHO improvement ladder (compaction-proof context)

**Read this first on resume.** This session's single mission: raise WHO
("who took the shot / did the event") by climbing a measured ladder of
improvements, SAM2 LAST (user directive — speed), refuting or adopting each
rung on the three-game scoreboard.

## The scoreboard (kept state, all committed)
| game | WHO | zone | ceiling (right track exists, any cam) |
|---|---|---|---|
| e6fba750 | **69%** | 87% | 85% |
| 2c490f1a (blind) | **63%** | 75% | 89% |
| c2a354fe (blind) | **40%** | 70% | 80% |

Score command per game (the harness):
```
.venv/bin/python scripts/detect_shots.py --game <gid8> \
  --plays data/plays/<gid8>_full.json --tracks-glob "runs/events_fg_<pfx>_{tag}" \
  --no-team-prior          # e6 uses default tracks-glob runs/events_fg_{tag}
```
(pfx = c2a / 2c4; e6 plain. --no-team-prior always: prior refuted at 52%.)

## The ladder (docs/RESEARCH_WHO_SOTA.md has sources)
1. **R1-full (IN FLIGHT)** — GTA-Link-style tracklet split/merge with our
   ft-KPR: stage 0 `scripts/r1_sample_frames.py --game c2a354fe` (S3 frames
   1/2s per cam -> per-stream crop timelines, runs/r1_samples_c2a/); stage
   1+2 `KPR_CFG=configs/kpr/imagenet/kpr_uball_test_cyc2.yaml
   /tmp/kpr/.venv310/bin/python scripts/r1_tracklet_refine.py --game
   c2a354fe` (embed timelines, median-smoothed FOREIGN-SEGMENT detection ->
   patches.json); stage 3 `scripts/r1_apply_patches.py --game c2a354fe`
   (move frame-ranges between stream dumps) -> re-run harness -> scoreboard.
   c2a first (scrum game). Gate: c2a +4 WHO, no regression elsewhere.
2. **R2** — SportSORT-style jersey/kit costs INSIDE ByteTrack association
   (hybrid_track.py); shades already computed at anchor time.
3. **SAM2 scrum-windows** — ONLY if R1/R2 leave the gap; ~5% of frames,
   McByte-style mask guidance.
4. Game 13e1ffad stays SEALED for final blind validation.

## Adopted this session (each cross-game positive)
- Kit-birth streams (hybrid_track --dual-numbers: n3B/n3W separate tracklets)
- Widened attribution window [rel-0.8s, rel+0.25s]
- Release-quality (rq) per arc + best-quality play matching
## Refuted this session (numbers in docs/WHO_MASTER_PLAN.md W-log)
anchor-sparsity gating · possession team-prior (52% coin flip) · 3 release
snap variants · rq-aware cam ORDERING · W4 grab-instant rebounds · fusion
(obsolete post-round-1) · W3 detector-box+KPR (absence self-closed) ·
R1-LITE single-crop veto (blurred-shooter bias — never decide on one crop)

## In-flight background tasks (check on resume)
- c2a frame sampling: /tmp/r1s_c2a.log -> runs/r1_samples_c2a/index.json
- 4-angle review videos: /tmp/seg4_redo.log -> overwrites
  runs/event_demo/demo5min_{e6fba750,c2a354fe}.mp4 (VERIFY a frame is not
  black before sending: earlier bug = unfinalized MP4 sources, now ffprobe-
  gated ≥295s)

## The tracking .md files
- docs/WHO_MASTER_PLAN.md — the plan + living W-log (every verdict)
- docs/RESEARCH_WHO_SOTA.md — literature map + ladder rationale
- docs/STATUS.md — client-facing; Part 9 + round-1 scoreboard section
- docs/C2A_BLIND_RESULTS.md, docs/2C4_BLIND_RESULTS.md — blind game reports
- docs/SESSION_CONTEXT.md — long-term operational truth (older eras + traps)
- docs/PAINT_WHO_PLAN.md — the original E-series (historical)

## Traps refreshed today
- Background clip cutting: VERIFY ffprobe duration before consuming (partial
  MP4 = no moov atom = cv2 reads black silently).
- e5-family harness matches nearest-in-time (reads ~3-6pts below the
  detect_shots rq-matching scorer) — compare like with like.
- KPR venv lives in /tmp and DECAYS (repair recipe in SESSION_CONTEXT).
- Events regen order: arc-windows builder -> adapter -> transfer_eval ->
  assemble_events -> detect_possession_events (possession cache delete first).

## User's standing instruction for the rest of this session
Run everything until SAM2 (exclusive). At every NON-refuted step report:
(a) accuracy increase per game, (b) what is still broken. Report format =
scoreboard delta + "still broken" list.


## SESSION OUTCOME (end of 2026-07-14)
Ladder run to completion INCLUDING SAM2 (4/20, refuted). Kept gains: e6
62->69/87, 2c4 59->63/75, c2a 37->40/70 (WHO/zone). Twelve approaches
measured-and-refuted (W-log). Conclusion: residual hard-crowd WHO is
information-limited in this footage; next round = user's 4-angle video
triage + venue levers (bigger numbers first). 13e1ffad remains sealed.
